# ไฟล์ตัวอย่าง — คัดลอกเป็น secrets.toml แล้วใส่ค่าจริง
# (อย่า commit secrets.toml ขึ้น GitHub เด็ดขาด)

[database]
# PostgreSQL connection string จาก Supabase
# Settings → Database → Connection string → URI
url = "postgresql://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres"
